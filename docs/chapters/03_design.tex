% II. Проектиране и програмна реализация.
\section{Проектиране и програмна реализация}
\label{sec:design}

Системата е разделена на четири слоя, всеки проверим самостоятелно: четене на байтове, разчитане
на протокола, проверка на веригата и допускане на връзки. Обемът по модули е в Приложението.

\subsection{Разчитане и граница на видимостта}
\label{subsec:parser}

Входът е \code{std::span<const std::uint8\_t>}, тоест невладеещ изглед към чужд буфер, което прави
измерването възпроизводимо. Всяко поле минава през четец, който проверява дължината
предварително; при недостиг четецът се маркира като отказал и причината за първия отказ се
запазва, без изключения: изкривеното съобщение е нормален вход за такъв код. Дължината на записа
се проверява срещу границата от
\cite{rfc8446}, раздел 5.1, преди копирането на какъвто и да е байт, иначе изпращачът решава колко
памет заделя получателят. Съдържанието на всички записи от вид \code{handshake} се слепва в един
буфер, преди да бъде нарязано на съобщения, защото едно съобщение може да се разпростре върху
няколко записа; разчитач, който приравнява запис на съобщение, сгрешава точно интересния случай.

В явен вид са само ClientHello и ServerHello: предложени и договорени версия, шифрова съвкупност,
група за ключова обмяна, схеми за подпис, име на сървъра и ALPN. Скрити остават Certificate,
CertificateVerify и Finished, а с тях сертификатните вериги на двете страни. Анализаторът
докладва наблюдаваното и изброява какво не е видял. Версията се чете от
\code{supported\_versions}, не от \code{legacy\_version}: по \cite{rfc8446} последното остава със
стойност за TLS 1.2 при всяка връзка по TLS 1.3, за да преминава през междинни устройства.
Разчитачът на DER отхвърля неопределена дължина, неминимално кодиране и високия номер на етикета:
разчитач, който приема няколко кодирания на една стойност, е начинът, по който две реализации
стигат до различно мнение какво пише в сертификата.

\subsection{Проверка на сертификатната верига}
\label{subsec:validator}

Първият етап изгражда пътя до доверен корен чрез обхождане в дълбочина по кандидат-издателите,
като разглежда всички продължения, а не само първото намерено: първият намерен път до корен често
не е този, който минава проверките. Вторият прилага проверките от \cite{rfc5280}
\tabref{tab:checks}, при което резултатът има три състояния вместо две: \code{PASS}, \code{FAIL}
и \code{SKIP}. Третото позволява да се каже коя проверка не е проведена и защо, вместо тя да бъде
преброена като успешна.

\begin{table}[htbp]
\caption{Проверки върху пътя и състоянието им при изрядна верига от три сертификата}
\label{tab:checks}
\centering
\fontsize{10}{12}\selectfont
\begin{tabular}{L{4.8cm} l L{5.8cm} c}
\toprule
Проверка & Раздел & Какво отхвърля & Състояние \\
\midrule
Свързване по име & 6.1 & прекъсване между издател и субект & \code{PASS} \\
Срок на валидност & 4.1.2.5 & изтекъл или още невлязъл в сила & \code{PASS} \\
Основни ограничения & 4.2.1.9 & издател, който не е удостоверител & \code{PASS} \\
Дължина на пътя & 4.2.1.9 & повече звена от позволеното & \code{PASS} \\
Предназначение на ключа & 4.2.1.3 & удостоверител без \code{keyCertSign} & \code{PASS} \\
Разширено предназначение & 4.2.1.12 & ключ, негоден за сървър & \code{PASS} \\
Съответствие на името & RFC 6125 & име, непокрито от \code{subjectAltName} & \code{PASS} \\
Критични разширения & 6.1 & неразпознато критично разширение & \code{PASS} \\
Отмяна & 5 & сериен номер в местния списък & \code{PASS} \\
Ограничения върху имената & 4.2.1.10 & име извън позволеното поддърво & \code{SKIP} \\
Подпис на издателя & 6.1 & подправен подпис & \code{SKIP} \\
\bottomrule
\end{tabular}
\end{table}

Двата реда със \code{SKIP} са най-важното в таблицата. Първият е такъв само по този път, тъй като
никой издател по него не ограничава имена. Вторият е постоянен: подписът не се проверява, както е
обяснено в Раздел~\ref{subsec:choices}, и всеки отчет носи ред \code{SKIP} с причината.
Съответствието на името следва RFC 6125, раздел 6.4.3: един заместващ знак, само в най-лявата
съставка, никога през точка, и поне две съставки след него.

\subsection{Допускане, стенд и тестове}
\label{subsec:admission_design}

Слоят е верига от четири независими механизма, всеки включван отделно. Първа е кофата с жетони по
източник, най-евтината проверка. Втора е бисквитката без състояние, \code{siphash24} върху
източника, стойността на клиента и груб времеви прозорец: при първи опит сървърът не заделя нищо,
а връща стойност, която клиентът трябва да върне и с това да докаже, че получава на адреса, който
твърди, че има. Трета е изчислителната задача, при която проверката у сървъра е едно хеширане, а
търсенето у клиента е средно $2^{k}$ хеширания при $k$ бита трудност. Едва четвърта е таблицата с
връзките, тоест заделянето на състояние; при запълване се изхвърля най-старата незавършена връзка,
защото при лавина тя почти сигурно е част от лавината. Политиката на плавно влошаване вдига цената
на първите три с нарастване на запълването.

Слушателят на стенда се свързва единствено с адреса на локалния интерфейс: генераторът на
натоварване не може да достигне нищо освен този процес. Списъкът с 82 теста се произвежда от
самия изпълним файл, за да не може да се размине с кода, а целият входен материал се генерира от
хранилището срещу \cite{rfc8446} и \cite{rfc5280}; сертификатите носят запълващ низ на мястото на
подписа, който никой не чете.

\subsection{Модел на заплахата и граници}
\label{subsec:threat_model}

Приема се, че наблюдателят няма частните ключове и не може да дешифрира защитената част от
обмена. \tabref{tab:threat} изброява срещу какво системата действа и срещу какво не действа.

\begin{table}[htbp]
\caption{Модел на заплахата: актьор, способност и мярка на системата}
\label{tab:threat}
\centering
\fontsize{10}{12}\selectfont
\begin{tabular}{L{3.7cm} L{4.4cm} L{6.6cm}}
\toprule
Актьор & Способност & Мярка \\
\midrule
Пасивен наблюдател & чете трафика, няма ключове &
извън обхвата: системата е на негово място \\
Неправомерен издател & подписва сертификат за чуждо име &
съответствие и ограничения върху имената, основни ограничения \\
Страна с негодна верига & подрежда произволни сертификати &
път до доверен корен, проверка на всяко звено \\
Подправен подпис при верни полета & преподписва съдържанието &
\textbf{няма мярка}: подписът не се проверява \\
Лавина от един адрес & евтино открива връзки &
кофа с жетони по източник, отчитане на таблицата \\
Разпределена лавина & открива връзки от много адреси &
бисквитка без състояние, задача, плавно влошаване \\
Нападател над машината или приложния слой & изпълнява код, злоупотребява над транспорта &
извън модела, вж. \cite{owasp_top10_2021} \\
\bottomrule
\end{tabular}
\end{table}

Мерките в третата колона са проектни решения, проверени от тестовете върху построени случаи, но не
срещу действителен нападател: системата не е подлагана на атака, на фъзинг и на външен одит.
